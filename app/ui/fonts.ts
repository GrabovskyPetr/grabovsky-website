import { Inter } from "next/font/google"
import { Titillium_Web } from "next/font/google"

export const inter = Inter({
    subsets: [ "latin" ]
})

export const silkscreen = Titillium_Web({
    subsets: [ "latin" ],
    weight: [ "400", "700" ]
})